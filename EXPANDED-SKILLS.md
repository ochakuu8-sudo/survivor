# 180ノード版のプレイテスト

## 起動

`npm install` → `npm run dev`。開始時と再挑戦時にスキルツリーが開く。

現在は `src/buildSettings.js` の `DEBUG_FREE_SKILLS = true` により、**全180ノードと60レシピの習得コストが0**。前提、有効20ノード、大ノード4個、通常武装3個、特殊ルール1個の制約は残る。共通の基礎成長は通常のG価格で検証する。

初期の習得・G・共通成長は0。旧保存 `survivor.progression.v1` は変更せず、新保存 `survivor.progression.v2.zero-start` を使用する。以後の習得・構成・Gはリトライと再読み込みを跨いで残る。

## 操作

- 移動：WASD、矢印、タッチ。攻撃は自動。
- K：スキルツリー。検索、領域選択、ドラッグ、ズーム、全体表示。
- 右側の60レシピから選び「このレシピへ振り直す」。必要経路の購入・返金を一括処理。
- 出撃前・ボス撃破後に取り外し・返金・プリセットの変更。戦闘中は追加習得・有効化。
- 6枠の保存構成。共通成長は構成を変えても有効。
- 開発サーバーでは右側の下に「習得・G・基礎成長を0へ」と「テスト用 +1000 G」。保存構成はリセットボタンで消さない。
- 通常の経済へ戻すときは `DEBUG_FREE_SKILLS = false`。無料で買ったノードを返金しても0G。

## 実装

- `skillCatalog.js`：144領域ノード、24接続ノード、12特殊ルール、60レシピ。
- `buildModel.js`：前提・容量・武装・排他検証、購入、返金、振り直し、共通成長。
- `buildCombat.js`：武装ごとの発射周期、弾倉、帰還、跳弾、軌道、設置、召喚、状態異常、条件付き攻撃。実判定を演出配列から分離。
- `skillTree.js`：円環ツリー、ノード詳細、検索、プレビュー、プリセット、実ゲームへの接続。
- `progression.js`：新保存領域、入力の検証、実支払額の記録。

## 検証と調整範囲

`npm test` と `npm run build`。24テストには全60レシピの戦闘シミュレーション、全180ノードの無料購入、通常価格と返金、保存、状態異常、弾倉、契約、罠、ループ境界、既存の5ボス進行を含む。

別途ブラウザで召喚爆撃の発動、レシピ操作、構成保存と再読み込み、390px画面の表示を確認。戦闘シミュレーションは動作成立の検証で、全60構成が同等の強さ、全ボスを同じ投資額で安定クリアすることを保証するものではない。威力・周期・個数・報酬は初期調整値。

形状プレビューは簡易アニメーション。実戦では既存の石スプライトと属性の色・軌道・地面領域を使用し、180種類の専用画像を必要としない。
